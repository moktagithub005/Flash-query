# Flash Quarry

A simple memory grid game. Memorize the flashed symbols, then tap the cells you remember.

## Run

1. Install dependencies.
2. Start the dev server.

```bash
cd flash-quarry
npm install
npm start
```
# Flash-query
